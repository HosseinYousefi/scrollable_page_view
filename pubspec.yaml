name: scrollable_page_view
description: Infinite horizontally scrollable pages inside a horizontal PageView.
version: 0.0.2
repository: https://github.com/HosseinYousefi/scrollable_page_view

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: